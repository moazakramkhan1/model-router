global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - "alerts.yml"

scrape_configs:
  - job_name: "router-api"
    static_configs:
      - targets: ["router-api:8000"]
    metrics_path: /metrics
